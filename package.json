{
  "name": "edustream-saas",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"nodemon server/server.js\"",
    "build": "npm run build --prefix client",
    "postinstall": "npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
